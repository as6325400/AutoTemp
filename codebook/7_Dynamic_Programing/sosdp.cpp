// 檢查第 𝑛 位是否為1
if(a & (1 << n))

// 強制將第 𝑛 位變成1
a |= (1 << n)

// 強制將第 𝑛 位變成0
a &= ~(1 << n)

// 將第 𝑛 位反轉(1變0, 0變1)
a ^= (1 << n)

// 第 0 ~ 𝑛 − 1位 全部都是1
(1 << n) - 1

// 兩個集合的聯集
S = a | b

// 兩個集合的交集
S = a & b